[workspace]
resolver = "2"
members = [
    #    "crates/gldf-rs-slint",
    "crates/gldf-bevy",
    "crates/gldf-rs-egui",
    "crates/gldf-rs-ffi",
    "crates/gldf-rs-lib",
    "crates/gldf-rs-python",
    "crates/gldf-rs-wasm",
    "crates/gldf-starsky-wasm",
    "crates/gldf-unreal",
    # gldf-stadium-plugin is parked: it still pulls crates.io Bevy 0.18,
    # which conflicts with gldf-bevy's `photometric_proposal` fork
    # (Bevy 0.19-dev). Re-add once stadium is migrated to the same fork.
    # "crates/gldf-stadium-plugin",
]

[workspace.package]
version = "0.4.0"
edition = "2021"
authors = ["Holger Trahe <trahe@mac.com>"]
license = "AGPL-3.0-or-later"
repository = "https://github.com/holg/gldf-rs"
homepage = "https://github.com/holg/gldf-rs"

[workspace.dependencies]
# Core library
gldf-rs = { version = "0.4.0", path = "crates/gldf-rs-lib", default-features = false }

# Serialization
quick-xml = { version = "0.37", features = ["serialize"] }
serde = { version = "1.0", features = ["derive"] }
serde-xml-rs = "0.6"
serde_json = "1.0"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Async runtime
async-std = "1.12"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "test-util", "macros"] }

# HTTP client
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }
reqwest-wasm = { version = "0.11", features = ["blocking"] }

# Compression - use zip 2.x for better compatibility
zip = { version = "2", default-features = false, features = ["deflate"] }

# Utilities
base64 = "0.22"
lazy_static = "1.5"
regex = "1.10"

# UI framework (egui)
eframe = { version = "0.33", default-features = false, features = [
  "accesskit",
  "default_fonts",
  "glow",
  "persistence",
] }
egui_extras = { version = "0.33", features = ["all_loaders", "image", "svg"] }

# Image processing
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
resvg = "0.45"

# 3D rendering
three-d = { version = "0.18", default-features = true }
three-d-asset = { version = "0.9", features = ["obj"] }

# Bevy 3D engine
bevy = { version = "0.18", default-features = false, features = [
    "bevy_asset",
    "bevy_render",
    "bevy_core_pipeline",
    "bevy_pbr",
    "bevy_winit",
    "bevy_gizmos",
    "png",
    "webgl2",
    "x11",
    "zstd_rust",
] }

# eulumdat-bevy for lighting plugins.
# Path-dep into the local eulumdat-rs workspace so we get exactly one
# eulumdat-bevy + one eulumdat in the graph. The local eulumdat-bevy
# pins Bevy to a custom fork branch (`photometric_proposal`); gldf-bevy
# matches that pin so the whole graph rides on one Bevy version too.
# Flip back to a published `eulumdat-bevy = "0.7.x"` once that ships.
eulumdat-bevy = { path = "../eulumdat-rs/crates/eulumdat-bevy" }

# Photometric data — local path so the upcoming 0.7 release rides along
# without a crates.io publish. Pure-eulumdat has no Bevy dep so this
# would be safe even with a mixed graph; we keep the path consistent
# with eulumdat-bevy above.
eulumdat = { path = "../eulumdat-rs/crates/eulumdat" }
# Family-shared i18n. The viewer's UI chrome (sidebar nav, toolbar
# buttons, headings, etc.) is translated via the `extra` flat-key
# section added in eulumdat-i18n 0.6.1. Same path-dep caveat as
# `eulumdat` above — flip back to crates.io for release.
eulumdat-i18n = { path = "../eulumdat-rs/crates/eulumdat-i18n" }
# Path-dep into the local l3d-rs workspace so we get the in-progress
# `fbx-export` feature (a 1400-line ASCII FBX 7.4 writer in
# crates/l3d_rs/src/fbx.rs that's not yet published). Used by
# gldf-unreal to emit FBX assets that Unreal Datasmith can ingest.
# Flip back to a crates.io version once l3d-rs ships an fbx-export
# release.
l3d_rs = { path = "../l3d-rs/crates/l3d_rs" }

# ULD/ROLF file conversion — parked. The legacy `../uld-rs` pulls
# `cfb 0.10` which pins `uuid = ^1` to 1.19.0; the local Bevy fork
# (photometric_proposal, 0.19-dev) needs `uuid ^1.21.0`, and Cargo
# can't satisfy both with a single uuid version. Live ULD work lives
# in `../light-other-rs` (on the `light-other` branch). Re-enable
# once the `uld` feature is rebound to that crate.
# uld-rs = { path = "../uld-rs", features = ["gldf"] }

# File dialogs (native)
rfd = "0.15"

# WASM
gloo = "0.11"
gloo-file = { version = "0.3", features = ["futures"] }
js-sys = "0.3"
# Pinned to match cargo-leptos 0.3.6's bundled wasm-bindgen-cli (0.2.114).
# Bumping requires `cargo install -f wasm-bindgen-cli --version <new>`
# on the dev box; pin upgrades wait until that's coordinated.
wasm-bindgen = "=0.2.114"
wasm-bindgen-futures = "0.4"
wasm-bindgen-test = "0.3"
web-sys = "0.3"
yew = { version = "0.22", features = ["csr"] }

# Leptos stack — 0.8.x. Each consumer picks the features it needs
# (csr/hydrate for the wasm bundle, ssr for a server binary).
console_error_panic_hook = "0.1"
leptos = { version = "0.8", default-features = false }
leptos_axum = { version = "0.8", default-features = false }
leptos_meta = { version = "0.8", default-features = false }
leptos_router = { version = "0.8", default-features = false }

# Python
pyo3 = { version = "0.24", features = ["extension-module", "abi3-py38"] }
pyo3-build-config = "0.24"

# FFI (UniFFI)
uniffi = { version = "0.28", features = ["cli"] }

# Logging
env_logger = "0.11"
log = "0.4"

# Build utilities
version_check = "0.9"

# [patch.crates-io] removed — eulumdat-bevy is now path-dep'd above, so
# crates.io eulumdat never enters the graph and there's nothing to patch.
